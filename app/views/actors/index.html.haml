%h1 Listing actors

%table
  %tr
    %th Name
    %th Pic
    %th
    %th
    %th

  - @actors.each do |actor|
    %tr
      %td= actor.name
      %td= actor.pic
      %td= link_to 'Show', actor
      %td= link_to 'Edit', edit_actor_path(actor)
      %td= link_to 'Destroy', actor, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Actor', new_actor_path
